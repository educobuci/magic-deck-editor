import { mock } from 'jest-mock-extended'

describe('ReactEditorPresenter', () => {
  it.todo('should format the deck data to the view', () => {
    // const view: IEditorView = mock<IEditorView>()
    // const presenter = new 
  })
})

export {}